# Retired

`pnpm run sandcastle` no longer reads this file. `main.ts` polls GitHub and runs `prompts/sdd.md` in a worker. The skill name is the only part that changes.

Phases and skills live in `skills/`. Capability cuts, review-thread commands, and the issue mirror live in `context.md`.
